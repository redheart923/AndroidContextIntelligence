# Native / JNI / Build Static Graph v0.1 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add deterministic C/C++/Rust symbols, JNI/FFI bindings, Soong Blueprint dependencies, and optional Ninja/build-artifact facts to partial Android workspaces without requiring a complete AOSP build.

**Architecture:** Parser adapters emit canonical typed facts; identity resolvers and cross-language linkers convert only proven facts into the staging graph while ambiguous results remain candidate audit nodes. Every capability participates in the existing execution plan, provenance, correction, validation, fingerprint, and atomic publication pipeline.

**Tech Stack:** Python 3.10+, SQLite, Universal Ctags, `tree-sitter==0.26.0`, `tree-sitter-c==0.24.2`, `tree-sitter-cpp==0.23.4`, `tree-sitter-rust==0.24.2`, pytest, Bash/WSL.

**Spec:** `doc/designs/2026-08-25-native-jni-build-static-graph-v01-design.md`

## Global Constraints

- `project/` remains the canonical deployable payload; root `setup.sh` installs it into `/home/ts/android-context-intelligence`.
- Ctags remains the deterministic broad symbol layer; Tree-sitter and future precision adapters augment it.
- Unknown syntax, missing repositories, unresolved conditions, and ambiguous JNI matches never become active semantic edges.
- Build inputs are consumed only when supplied and are never generated by this pipeline.
- Real-AOSP acceptance remains `NOT RUN` until a complete source/build workspace is available.
- Every parser/tool/profile identity and input fingerprint participates in provenance and cache invalidation.
- Each task starts with a failing test, ends with focused and regression tests, and produces one reviewable commit.

---

## File Map

New packages:

```text
project/collectors/facts/       typed fact contracts and canonical JSONL
project/collectors/native/      C/C++/Rust adapters, resolvers and materializer
project/collectors/build/       Blueprint, Soong, Ninja and artifact adapters
project/collectors/interop/     managed native declarations and JNI/FFI linker
project/workspace/native_pipeline.py
project/workspace/native_validation.py
project/workspace/build_inputs.py
```

Existing integration points:

```text
project/config/parser_registry.toml
project/workspace/models.py
project/workspace/planner.py
project/workspace/coverage_validation.py
project/workspace/provenance.py
project/workspace/source_scope_validation.py
project/scripts/rebuild_all.sh
project/storage/migrations/
project/collectors/codeql/corrections.py
project/requirements-lock.txt
```

## Task 1: Canonical Typed-Fact Contract

**Files:**
- Create: `project/collectors/facts/__init__.py`
- Create: `project/collectors/facts/model.py`
- Create: `project/collectors/facts/codec.py`
- Create: `project/collectors/facts/identity.py`
- Test: `project/tests/unit/test_typed_facts.py`

**Interfaces:**
- Produces: `SourceRange`, `Evidence`, `SymbolFact`, `RelationFact`, `BuildModuleFact`, `BuildActionFact`, `InteropBindingFact`, `CandidateFact`, `DiagnosticFact`.
- Produces: `fact_identity(fact) -> str`, `write_facts(path, facts) -> str`, `read_facts(path) -> tuple[Fact, ...]`.

- [ ] **Step 1: Write failing canonicalization tests**

```python
def test_fact_identity_is_order_independent() -> None:
    left = SymbolFact("cpp", "CPP_FUNCTION", "android::start", location(),
                      evidence(), {"flags": ["b", "a"]})
    right = replace(left, properties={"flags": ["a", "b"]})
    assert fact_identity(left) == fact_identity(right)

def test_jsonl_round_trip_preserves_typed_fact(tmp_path: Path) -> None:
    path = tmp_path / "facts.jsonl"
    digest = write_facts(path, [sample_symbol()])
    assert read_facts(path) == (sample_symbol(),)
    assert len(digest) == 64
```

- [ ] **Step 2: Run `python -m pytest -q tests/unit/test_typed_facts.py` and verify import failure.**
- [ ] **Step 3: Implement frozen dataclasses, enum validation, normalized POSIX paths, sorted set-like properties, canonical JSON and SHA-256 identities.**
- [ ] **Step 4: Run the focused test and `python -m pytest -q`; expect all tests to pass.**
- [ ] **Step 5: Commit with `git commit -m "feat(facts): add typed extraction contracts"`.**

## Task 2: Workspace Model, Version Identity and Schema Scaffold

**Files:**
- Modify: `project/workspace/models.py`
- Modify: `project/workspace/planner.py`
- Modify: `project/workspace/cli.py`
- Modify: `project/workspace/source_scope_validation.py`
- Create: `project/workspace/platform_identity.py`
- Create: `project/storage/migrations/0003_native_build_candidates.sql`
- Test: `project/tests/unit/test_native_workspace.py`
- Test: `project/tests/unit/test_schema_migrations.py`

**Interfaces:**
- Changes `WorkspacePlan.strict_capabilities` to `tuple[str, ...]`; JSON uses `strict_capabilities` and reads legacy `strict_capability` during migration.
- Produces `PlatformIdentity(name: str, source: str, fingerprint: str)` and `detect_platform_identity(aosp_root, override) -> PlatformIdentity`.
- Adds candidate indexes/views without changing existing node/edge meanings.

- [ ] **Step 1: Add failing tests for repeated strict capabilities, legacy-plan loading, unknown partial platform identity, migration idempotence, and candidate exclusion from `effective_node`.**

```python
assert plan.strict_capabilities == ("jni_bindings", "native_symbols")
assert detect_platform_identity(root, None).name == "unknown"
assert connection.execute(
    "SELECT count(*) FROM effective_node WHERE node_type='EXTRACTION_CANDIDATE'"
).fetchone()[0] == 0
```

- [ ] **Step 2: Run focused tests and verify the new fields/migration are absent.**
- [ ] **Step 3: Implement compatibility parsing, platform metadata detection, migration tables/indexes, and candidate-effective-view exclusion.**
- [ ] **Step 4: Run schema, planner, scope and coverage tests plus the full suite.**
- [ ] **Step 5: Commit with `git commit -m "feat(workspace): add native graph version scaffold"`.**

## Task 3: C/C++ Ctags Symbol Baseline

**Files:**
- Create: `project/collectors/native/__init__.py`
- Create: `project/collectors/native/ctags_symbols.py`
- Create: `project/collectors/native/identity.py`
- Modify: `project/workspace/languages.py`
- Test: `project/tests/unit/test_native_ctags_symbols.py`
- Fixture: `project/tests/fixtures/native/cpp/include/demo/Foo.h`
- Fixture: `project/tests/fixtures/native/cpp/src/Foo.cpp`
- Fixture: `project/tests/fixtures/native/c/src/demo.c`

**Interfaces:**
- Produces `ctags_command(root, language, output) -> list[str]`.
- Produces `decode_ctags_record(record, repository) -> tuple[SymbolFact, ...]` for `C_FUNCTION`, `C_TYPE`, `C_MACRO`, `CPP_NAMESPACE`, `CPP_TYPE`, `CPP_FUNCTION`, `CPP_METHOD`, `CPP_FIELD`.

- [ ] **Step 1: Write failing tests with checked-in Ctags JSON records for namespace/class/method, C function, macro, overloaded functions, and duplicate definitions in two repositories.**
- [ ] **Step 2: Verify tests fail because native kinds are unsupported.**
- [ ] **Step 3: Implement deterministic qualified-name/signature mapping and emit `SYMBOL_DEFINITION`-compatible facts without direct SQLite writes.**
- [ ] **Step 4: Run native symbol tests, collision tests and full regression tests.**
- [ ] **Step 5: Commit with `git commit -m "feat(native): add c and cpp ctags symbols"`.**

## Task 4: Tree-sitter C/C++ Structure Parser

**Files:**
- Modify: `project/requirements-lock.txt`
- Create: `project/collectors/native/treesitter_runtime.py`
- Create: `project/collectors/native/cpp_syntax.py`
- Create: `project/collectors/native/header_language.py`
- Test: `project/tests/unit/test_cpp_syntax.py`
- Test: `project/tests/unit/test_header_language.py`

**Interfaces:**
- Produces `parse_cpp_file(path, language, repository, revision) -> ParseResult`.
- Produces `resolve_header_language(path, compile_db, owning_modules) -> Literal["c", "cpp"] | None`.
- `ParseResult` contains `facts`, `diagnostics`, grammar name and grammar fingerprint.

- [ ] **Step 1: Add the four exact Tree-sitter pins to `requirements-lock.txt` and write failing tests for namespace, inheritance, methods, includes, parse errors, and ambiguous `.h`.**
- [ ] **Step 2: Install the locked requirements in the isolated venv and verify the focused tests fail on missing adapters.**
- [ ] **Step 3: Implement parser construction and AST walks; `.h` without unique compile/Soong context emits `ambiguous_header_language` and no grammar-dependent relation.**
- [ ] **Step 4: Run focused tests twice and assert identical fact JSON and parser fingerprints, then run the full suite.**
- [ ] **Step 5: Commit with `git commit -m "feat(native): add c cpp structural parser"`.**

## Task 5: Rust Symbols, Types and FFI

**Files:**
- Create: `project/collectors/native/rust_syntax.py`
- Create: `project/collectors/native/rust_resolver.py`
- Test: `project/tests/unit/test_rust_syntax.py`
- Fixture: `project/tests/fixtures/native/rust/src/lib.rs`

**Interfaces:**
- Produces `parse_rust_file(path, crate_context, repository, revision) -> ParseResult`.
- Emits `RUST_CRATE`, `RUST_MODULE`, `RUST_TYPE`, `RUST_TRAIT`, `RUST_IMPL`, `RUST_FUNCTION`, `RUST_MACRO_INVOCATION`, `IMPLEMENTS_TRAIT`, `IMPLEMENTS_TYPE`, `IMPORTS_SYMBOL`, `EXPORTS_C_ABI_SYMBOL`.

- [ ] **Step 1: Write failing tests for nested modules, `use`, trait impl, inherent impl, `extern "C"`, `no_mangle`, `export_name`, `link_name`, unknown cfg and unexpanded macro.**
- [ ] **Step 2: Verify the tests fail on missing Rust parser.**
- [ ] **Step 3: Implement AST extraction; unknown cfg and macros emit diagnostics/candidates rather than unconditional facts.**
- [ ] **Step 4: Run Rust, typed-fact and deterministic fingerprint tests plus the full suite.**
- [ ] **Step 5: Commit with `git commit -m "feat(native): add rust symbols types and ffi"`.**

## Task 6: Blueprint Parser and Deterministic Soong Evaluation

**Files:**
- Create: `project/collectors/build/__init__.py`
- Create: `project/collectors/build/blueprint_lexer.py`
- Create: `project/collectors/build/blueprint_parser.py`
- Create: `project/collectors/build/soong_resolver.py`
- Test: `project/tests/unit/test_blueprint_parser.py`
- Test: `project/tests/unit/test_soong_resolver.py`
- Fixture: `project/tests/fixtures/native/build/Android.bp`

**Interfaces:**
- Produces `parse_blueprint(text, source_path) -> BlueprintDocument`.
- Produces `resolve_soong(documents, repositories) -> SoongResolution` with `modules`, `relations`, `candidates`, `diagnostics`.

- [ ] **Step 1: Write lexer/parser tests for comments, strings, list/map nesting, variables, `+=`, trailing commas and exact source ranges.**
- [ ] **Step 2: Write resolver tests for defaults merge, filegroup expansion, genrule, cc/rust/java dependencies, duplicate module, missing module, defaults cycle and unresolved `select`/product/arch properties.**
- [ ] **Step 3: Run tests and verify missing parser failures.**
- [ ] **Step 4: Implement tokenizer, recursive-descent AST and deterministic resolver; preserve `raw_properties`, `resolved_properties`, `unresolved_expressions`.**
- [ ] **Step 5: Run focused and full tests; commit with `git commit -m "feat(soong): add blueprint static build graph"`.**

## Task 7: Optional Ninja and Build-Metadata Inputs

**Files:**
- Create: `project/workspace/build_inputs.py`
- Create: `project/collectors/build/ninja_parser.py`
- Create: `project/collectors/build/compile_commands.py`
- Create: `project/collectors/build/rust_project.py`
- Create: `project/collectors/build/module_info.py`
- Create: `project/config/build_inputs.local.toml.example`
- Test: `project/tests/unit/test_build_inputs.py`
- Test: `project/tests/unit/test_ninja_parser.py`
- Fixture: `project/tests/fixtures/native/build/build.ninja`
- Fixture: `project/tests/fixtures/native/build/compile_commands.json`
- Fixture: `project/tests/fixtures/native/build/rust-project.json`
- Fixture: `project/tests/fixtures/native/build/module-info.json`

**Interfaces:**
- Produces `load_build_inputs(path) -> tuple[BuildInput, ...]`.
- Produces adapter facts for `BUILD_RULE`, `BUILD_ACTION`, `BUILD_ARTIFACT`, translation units and crates.
- Stores command SHA-256 and allowlisted flags only.

- [ ] **Step 1: Write failing tests for optional missing inputs, hash mismatch, Ninja variables/rules/build/phony/include/subninja/implicit/order-only edges, duplicate output producers, and secret-bearing command redaction.**
- [ ] **Step 2: Verify failures before implementation.**
- [ ] **Step 3: Implement config validation, dedicated Ninja state machine and JSON adapters; reject revision-mismatched artifacts.**
- [ ] **Step 4: Run build-input, provenance and source-scope tests plus the full suite.**
- [ ] **Step 5: Commit with `git commit -m "feat(build): add ninja and build metadata adapters"`.**

## Task 8: Managed Native Declarations and JNI Name Codec

**Files:**
- Create: `project/collectors/interop/__init__.py`
- Create: `project/collectors/interop/managed_native_scanner.py`
- Create: `project/collectors/interop/jni_name_codec.py`
- Test: `project/tests/unit/test_managed_native_scanner.py`
- Test: `project/tests/unit/test_jni_name_codec.py`
- Fixture: `project/tests/fixtures/native/managed/Demo.java`
- Fixture: `project/tests/fixtures/native/managed/Demo.kt`

**Interfaces:**
- Produces `scan_managed_native(path, language, repository, revision) -> ParseResult`.
- Produces `decode_jni_symbol(symbol) -> DecodedJniName` and `encode_jni_name(class_name, method_name, descriptor) -> str`.

- [ ] **Step 1: Write failing tests for Java `native`, Kotlin `external fun`, arrays/inner classes, overloads, descriptors, `_1`, `_2`, `_3`, `_0xxxx`, short and long JNI names.**
- [ ] **Step 2: Verify codec/scanner imports fail.**
- [ ] **Step 3: Implement structural declaration scanning and a reversible JNI codec; reject malformed escapes rather than approximate-match.**
- [ ] **Step 4: Run focused tests, existing Java/Kotlin identity tests and the full suite.**
- [ ] **Step 5: Commit with `git commit -m "feat(jni): add managed declarations and name codec"`.**

## Task 9: Explicit JNI Registration and Cross-language Linker

**Files:**
- Create: `project/collectors/interop/jni_cpp_scanner.py`
- Create: `project/collectors/interop/rust_ffi_scanner.py`
- Create: `project/collectors/interop/linker.py`
- Test: `project/tests/unit/test_jni_registration.py`
- Test: `project/tests/unit/test_jni_linker.py`
- Fixture: `project/tests/fixtures/native/cpp/src/jni_registration.cpp`

**Interfaces:**
- Produces `scan_native_registrations(parse_results) -> tuple[InteropBindingFact | CandidateFact, ...]`.
- Produces `link_interop(managed, native, registrations, build_context) -> LinkResult`.

- [ ] **Step 1: Write failing tests for `JNINativeMethod`, `RegisterNatives`, Android helper registrations, direct JNI names, Rust export names, unique short match, overloaded short ambiguity, descriptor mismatch, missing managed source and conflicting evidence.**
- [ ] **Step 2: Verify no active binding is produced before linker implementation.**
- [ ] **Step 3: Implement explicit-registration-first precedence; build context may disambiguate but never independently prove a binding.**
- [ ] **Step 4: Assert every active binding has reversible evidence and every ambiguous case emits `EXTRACTION_CANDIDATE`; run full tests.**
- [ ] **Step 5: Commit with `git commit -m "feat(jni): link managed native and ffi symbols"`.**

## Task 10: Materialization, Candidate Corrections and Validation

**Files:**
- Create: `project/collectors/native/materializer.py`
- Create: `project/collectors/build/materializer.py`
- Create: `project/collectors/interop/materializer.py`
- Create: `project/workspace/native_validation.py`
- Create: `project/workspace/correction_replay.py`
- Modify: `project/collectors/codeql/corrections.py`
- Modify: `project/workspace/codeql_import.py`
- Modify: `project/storage/migrations/0003_native_build_candidates.sql`
- Test: `project/tests/unit/test_native_materializer.py`
- Test: `project/tests/unit/test_native_validation.py`
- Test: `project/tests/unit/test_fact_corrections.py`

**Interfaces:**
- Produces `materialize_*_facts(db, facts, run_id) -> MaterializationReport`.
- Produces `validate_native_graph(db, reports, strict_capabilities) -> ValidationReport`.
- Produces `replay_graph_corrections(db, corrections_dir, run_ids) -> CorrectionReport` as the single post-import correction stage for CodeQL and native facts.
- Adds correction actions `promote_candidate`, `suppress_candidate`, `replace_binding` while retaining existing actions.

- [ ] **Step 1: Write failing endpoint, duplicate exporter, candidate isolation, stale correction, invalid promotion, Soong missing-target and Ninja conflicting-producer tests.**
- [ ] **Step 2: Verify current effective views/corrections do not support the new cases.**
- [ ] **Step 3: Implement materializers through `GraphWriter`, candidate audit nodes, endpoint matrices, revision-bound correction handling and structured validation reports; remove correction side effects from `codeql_import` and replay all corrections once after every raw importer completes.**
- [ ] **Step 4: Run correction, migration, FK, collision, scope, fingerprint and full regression suites.**
- [ ] **Step 5: Commit with `git commit -m "feat(graph): validate and correct native build facts"`.**

## Task 11: Atomic Pipeline, Capabilities, Queries and Upgrade Fixtures

**Files:**
- Create: `project/workspace/native_pipeline.py`
- Modify: `project/config/parser_registry.toml`
- Modify: `project/workspace/planner.py`
- Modify: `project/workspace/coverage_validation.py`
- Modify: `project/workspace/provenance.py`
- Modify: `project/scripts/rebuild_all.sh`
- Create: `project/queries/native_capability_summary.sql`
- Create: `project/queries/jni_binding_summary.sql`
- Create: `project/queries/soong_native_module_summary.sql`
- Create: `project/tests/integration/test_native_atomic_rebuild.py`
- Create: `project/tests/fixtures/native/versions/legacy-known/Android.bp`
- Create: `project/tests/fixtures/native/versions/current-known/Android.bp`
- Create: `project/tests/fixtures/native/versions/future-unknown/Android.bp`

**Interfaces:**
- CLI accepts repeatable `--strict-capability` and `--build-inputs FILE`.
- Pipeline writes reports below staging `raw/native`, `raw/build`, `raw/interop`; publication happens only after native validation.

- [ ] **Step 1: Write failing integration tests for successful partial publication, parser failure rollback, optional build-input absence, repeated strict gates, candidate exclusion, provenance fingerprints and deterministic second build.**
- [ ] **Step 2: Add version fixtures proving legacy/current support and future-unknown degradation without active false edges.**
- [ ] **Step 3: Implement pipeline ordering, registry evidence, coverage states, provenance identities, CLI options and summary queries.**
- [ ] **Step 4: Run `python -m pytest -q`, `bash -n scripts/rebuild_all.sh`, `git diff --check`, and two fixture rebuilds with equal semantic fingerprints.**
- [ ] **Step 5: Commit with `git commit -m "feat(pipeline): publish native jni and build graph"`.**

## Task 12: Canonical Deployment, Documentation and WSL Partial Acceptance

**Files:**
- Modify: `project/README.md`
- Modify: `project/INSTALLATION_MANIFEST.txt`
- Modify: `README.md`
- Modify: `scripts/project_payload.py`
- Modify: `scripts/install_project.py`
- Modify: `tests/test_documentation_contract.py`
- Modify: `tests/test_project_payload.py`
- Modify: `tests/test_install_project.py`
- Create: `doc/reviews/2026-08-25-native-jni-build-static-graph-v01-acceptance.md`

**Interfaces:**
- Documents source config, build-input config, strict gates, reports, queries, upgrade behavior and exact `NOT RUN` boundaries.

- [ ] **Step 1: Write failing documentation/payload contract assertions for `native_symbols`, `jni_bindings`, `soong_build_graph`, `--build-inputs`, candidate isolation and real-AOSP `NOT RUN`.**
- [ ] **Step 2: Update canonical documentation, installation manifest and installer exclusions; preserve `config/build_inputs.local.toml` across upgrades and exclude it from payload hash comparison exactly like `source_roots.local.toml`. Do not add generated DB/cache/local config to payload.**
- [ ] **Step 3: Run root and project test suites, setup verify-only, payload sync and shell syntax checks.**
- [ ] **Step 4: Deploy in WSL with root `setup.sh --upgrade`, run partial `--plan-only`, rebuild, strict native/Soong/JNI fixture gates, FK check and summary queries. Record literal commands/output in the acceptance document.**
- [ ] **Step 5: Mark fixture and WSL partial results separately; keep full AOSP, generated Soong/Ninja and precision native call/dataflow acceptance as `NOT RUN`.**
- [ ] **Step 6: Commit with `git commit -m "docs: accept native jni build static graph v0.1"`.**

## Final Review and Integration

- [ ] Run `python -m pytest -q` at repository root and under `project/`.
- [ ] Run `git diff --check`, `bash -n setup.sh`, and `bash -n project/scripts/rebuild_all.sh`.
- [ ] Run `PROJECT_ROOT=/home/ts/android-context-intelligence bash setup.sh --verify-only` in WSL.
- [ ] Review every new supported capability against produced evidence and downgrade any capability lacking evidence.
- [ ] Request code review, address findings one commit at a time, and rerun the full gates.
- [ ] Merge the clean reviewed branch to `main`, verify `main`, then remove only the merged clean worktree.
